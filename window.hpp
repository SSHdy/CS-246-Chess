#pragma once
#define CS246CHESS_WINDOW_HPP

/*
#include <X11/Xlib.h>
#include <X11/Xutil.h>
#include <string>
#include <iostream>
#include <vector>

class Xwindow {
    Display *d;
    Window w;
    int s;
    GC gc;
    unsigned long colours[10];
    int width, height;

public:
    Xwindow(int width = 500, int height = 500);
    ~Xwindow();

    enum { White = 0, Black, Red, Green, Blue, Cyan, Yellow, Magenta, Orange, Brown };
    void fillRectangle(int x, int y, int width, int height, int colour);
    void drawString(int x, int y, const std::string &msg, int colour);
    void drawBigString(int x, int y, const std::string &msg, int colour);
};
*/